import React from 'react';
import useSomething from '@/hooks/useSomething';

const HeroSection = () => {
    return (
        <div>
            hero
        </div>
    );
};

export default HeroSection;